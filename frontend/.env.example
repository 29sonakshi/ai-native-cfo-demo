# Frontend build-time config (Vite). Copy to `.env` and set for your workspace,
# then rebuild: `npm run build`.

# Your workspace's Genie One URL — powers the "Open in Genie One" launchpad button.
# If unset, the button is hidden.
VITE_GENIE_ONE_URL=https://<your-workspace-host>/one
